services:
  - type: web
    name: deskflow-backend
    env: node
    plan: free
    root: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        scope: build,runtime
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
